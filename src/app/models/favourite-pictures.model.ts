export class HistoryRecord {
    //input: String
    selectedPicture: any

    constructor(selectedPicture: any) {
        //this.input = input;
        this.selectedPicture = selectedPicture;
    }
}